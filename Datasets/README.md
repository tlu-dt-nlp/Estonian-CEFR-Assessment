The train and test split of the main dataset used in the classification experiments are given in `train_split.csv` and `test_split.csv`.

The `train_test_splitter.py` script can be used to obtain new train-test splits of the combined dataset `train_test.csv`

Feature descriptions are listed in `feat_descriptions.txt`.